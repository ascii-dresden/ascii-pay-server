<!DOCTYPE html>
<html>

{{> _head title="Change password" }}

<body>
    <div class="container grid-lg">
        <div class="col-6 col-mx-auto col-sm-12">
            <div class="card card-top-padding">
                <div class="card-header">
                    <div class="card-title h1">
                        <img src="/images/ascii-pay-logo-wide.svg">
                    </div>
                </div>
                <form method="POST">
                    <div class="card-body">
                        <div class="form-group">
                            <label class="form-label" class="form-label" for="old_password">Old password</label>
                            <input class="form-input" type="password" name="old_password" />
                        </div>
                        <div class="form-group">
                            <label class="form-label" class="form-label" for="new_password">New password</label>
                            <input class="form-input" type="password" name="new_password" />
                        </div>
                        <div class="form-group">
                            <label class="form-label" class="form-label" for="new_password2">Repeat new password</label>
                            <input class="form-input" type="password" name="new_password2" />
                        </div>
                    </div>
                    <div class="card-footer">
                        <input class="btn btn-primary" type="submit" value="Change password" />
                        <a class="btn" href="/settings">Cancel</a>
                    </div>
                    <div class="card-footer">
                        {{#if error}}<div class="toast toast-error">Incorrect username or password!</div>{{/if}}
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

</html>